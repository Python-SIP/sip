To create and install the extension module, run:

    sip5-install
or
    pip install .

To create and install an sdist, run::

    sip5-sdist
    pip install fib-1.0.tar.gz

To create and install a wheel, run::

    sip5-wheel
    pip install fib-1.0-*.whl

To uninstall the extension module, run:

    pip uninstall fib
